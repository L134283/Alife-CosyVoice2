# CosyVoice2 语音合成

基于本地 **CosyVoice2（yinmei）** 的 Alife 语音合成插件。由插件自动拉起本地 Python 服务，对接 `ISpeechModel`，供语音输出链路使用。

> 依赖：`Alife.Function.AIModelUtility`（提供 `ISpeechModel` 接口，4.0 起由 `Alife.Function.Speech` 消费）  
> 本地服务：yinmei-cosyvoice（需自行安装模型与 Python 环境）

## 功能

| 功能 | 说明 |
|------|------|
| **本地 TTS** | 调用本机 CosyVoice2 API 合成 wav |
| **共享服务** | 多桌宠共用一个 Python TTS 进程（默认关）；跨进程串行合成 |
| **自动启动** | 可自动拉起 `start.py`，也可复用已在跑的服务 |
| **健康检查 / 守护** | 周期探测；进程存活视为仍在加载不杀（机械硬盘友好）；进程已死/僵尸才清理重启；共享时不误杀其它桌宠在用的服务 |
| **残留清理** | 按 PID 文件 + 端口查杀进程树；独占模式绑 Windows Job，共享模式不绑 |
| **手动服务控制** | 桌宠未启动也能在配置页手动「启动/停止」TTS，实时状态轮询；手动启动不随 Alife 退出，机械硬盘用户可后台预加载模型再开桌宠 |
| **音色列表** | 从 `/get_spk` 拉取；UI 可刷新；无效音色自动回退 |
| **风格指令** | 可选 `instruct`；**留空则不传**，使用音色默认语气（不会按文案自动换情绪） |
| **语速** | `speed`，1.0 为正常 |
| **请求重试** | 服务未就绪 / 真忙时短退避重试；不可朗读文本不请求 |
| **同句缓存** | 相同文本+音色+语速+指令会复用已生成文件；并发同句去重 |
| **取消联动** | 调用方取消且无人等待时，停止后台 InFlight 重试 |
| **预热** | 就绪后合成一句「你好」，降低首句延迟 |
| **配置 UI** | 音色、语速、风格、路径、端口、模式、共享服务等可视化配置 |
| **干净日志** | 控制台仅输出 `[Cosy语音] …`，无 `info: Namespace.Class[0]` 前缀 |
| **安静日志** | `QuietLog` 默认开：日常不刷合成过程，仅错误与重要状态（就绪/重启/停服） |
| **简洁可靠** | 无生图兼容代码，专注 TTS 核心：共享/看门狗/自动恢复/多桌宠 |

## 快速上手

1. 安装并准备好 **yinmei-cosyvoice**（Python、项目目录、CosyVoice2 模型）
2. 在角色配置中启用 **语音相关模块**，并启用本插件「Cosy语音合成」
3. 在插件 UI 中填写：
   - Python 路径
   - 项目目录（含 `start.py`）
   - 模型目录
   - API 端口（默认 `9981`）
4. 建议 **启动模式 = 1（仅 API）**，省资源
5. 多桌宠时开启 **共享服务 SharedService**（共用一个 TTS 进程）
6. 保持 **自动启动** 开启（若你已手动起服务，可关掉）
7. 选择音色、语速；风格指令按需填写
8. 保存配置后重启/重载角色，看到日志就绪即可

> **机械硬盘加载慢？** 在插件配置页顶部点「启动服务」让模型在后台慢慢加载（不随 Alife 退出），之后开桌宠即可秒用；或关闭「自动启动」，需要语音时手动点启动。

### 双桌宠共用一个 TTS（推荐）

两个 Alife 桌宠都启用本插件时：

1. **同一 API 端口**（默认 `9981`）
2. **SharedService = 开**（默认）
3. **Mode = 1**，不要再手动叠开 `start.bat`
4. 先开的桌宠拉起 Python；后开的日志会出现「检测到已有服务，复用中」
5. 关掉其中一个桌宠时，TTS 进程会保留；**两个都关** 后才停服

注意：本地 Cosy 单路推理，两桌宠同时说话会**排队串行**，不会各起一份 6GB。

### 就绪日志示例

**安静日志开启时（默认）** 大致只会看到：

```
[Cosy语音] 已启动（模式 1，独占）
[Cosy语音] 服务就绪，音色 21 个
```

**关闭安静日志** 后会额外输出合成过程、锁等待、预热等调试信息，例如：

```
[Cosy语音] 共享模式：客户端 1 个，端口 9981
[Cosy语音] 已启动（模式 1，共享）
[Cosy语音] 服务启动中…（共享，不绑定退出杀进程）
[Cosy语音] 等待模型加载…（1）
[Cosy语音] 预热完成
[Cosy语音] 服务就绪，音色 21 个
```

## 配置说明

| 配置 | 默认 | 说明 |
|------|------|------|
| API 地址 | `http://127.0.0.1:9981` | 与端口一致时会自动对齐 |
| 音色 | `女主持` | 对应 `/get_spk` 名称 |
| 语速 | `1.0` | 大于 0 |
| 风格指令 | 空 | 如「用开心的语气说」；**空 = 不传 instruct，默认语气** |
| Python 路径 | 本机路径 | yinmei 自带 `python.exe` |
| 项目目录 | 本机路径 | 含 `start.py` |
| 模型目录 | 本机路径 | 如 `pretrained_models/CosyVoice2-0.5B` |
| API 端口 | `9981` | TTS / 音色接口 |
| WebUI 端口 | `9980` | 仅 mode 2/3 |
| 启动模式 | `1` | `1` 仅 API（推荐）/ `2` 仅 WebUI / `3` 都开 |
| 自动启动 | 开 | 关则只连接已有服务 |
| 共享服务 | 关 | 多桌宠共用一个 Python；退出仅最后客户端停服 |
| 安静日志 | 开 | 开=仅错误与重要状态；关=完整调试日志 |
| 请求超时 | `90` 秒 | 单次合成 HTTP 超时 |
| 就绪超时 | `180` 秒 | 等待模型加载上限 |
| 失败重启阈值 | `8` | 健康检查连续失败次数后重启 |
| 合成锁超时 | `60` 秒 | 持锁过久视为 GPU 挂死并恢复 |

### 风格指令说明

| 填写情况 | 行为 |
|----------|------|
| **留空** | 请求不带 `instruct`，音色默认语气，相对稳定 |
| **填写** | 按指令控风格，如「用温柔的语气」「用开心的语气说」 |

插件**不会**根据文案内容自动选择情绪。

### 不可朗读文本

Cosy 服务对纯标点 / 空白（如 `...`、`！！！`、`——`）会稳定返回 500。  
插件在合成前会过滤此类文本，日志示例：

```
[Cosy语音][警告] 跳过不可朗读文本：「...」
```

## 依赖与环境

- **Alife 依赖**：`Alife.Function.AIModelUtility`（提供 `ISpeechModel`；4.0 起语音播放由 `Alife.Function.Speech` 消费）
- **本机环境**（不由插件 pip 安装）：
  - yinmei-cosyvoice 项目
  - 对应 Python 解释器
  - CosyVoice2 模型文件
- 默认路径示例（请按本机修改）：

```
Python：D:\1TTS\yinmei-cosyvoice\python311\python.exe
项目：  D:\1TTS\yinmei-cosyvoice
模型：  D:\1TTS\yinmei-cosyvoice\pretrained_models\CosyVoice2-0.5B
```

## 日志

控制台格式：

```
[Cosy语音] …
[Cosy语音][警告] …
[Cosy语音][错误] …
```

| QuietLog | 行为 |
|----------|------|
| **开（默认）** | 仅错误、警告、重要状态（就绪/重启/停服/手动清理） |
| **关** | 额外输出合成过程、锁等待、预热、健康检查细节等 |

## 版本

### v4.1.3

- **空闲内存剪枝（借鉴 GptSovits 插件的 EmptyWorkingSet 技术）**：新增「空闲时修剪 TTS 进程工作集」功能（默认开启）。空闲超过阈值（默认 10 分钟，可在配置页调整）后，把 TTS 进程的物理内存（Working Set）压回分页文件，显著降低任务管理器中的内存占用；下次合成时自动重新调入，不影响音质与功能。只降 RAM，不释放 GPU 显存
- 配置页新增「内存优化（空闲剪枝）」分区：开关 + 空闲分钟数可调

### v4.1.2

- **内存优化**：启动 Python 服务时新增 `CUDA_MODULE_LOADING=LAZY`（CUDA 模块按需加载，PyTorch 官方推荐）与 `MKL_DYNAMIC=TRUE`（MKL 线程缓冲按需分配），降低 Python 进程常驻内存
- **调查结论（供参考）**：yinmei 打包的 `start_model.pyd` 已硬编码以 FP16 加载模型（传参无效），显存 2.5GB 即 FP16 收益；Python 进程实际物理内存占用很低（约 130MB 工作集），任务管理器显示的较大内存多为虚拟地址空间/提交内存，不占真实物理 RAM

### v4.1.1

- **修复激活/热重载误杀加载中的服务**：端口有存活进程但未就绪（手动预加载、机械硬盘加载中）时不再 `ForceCleanup` 打断，交给看门狗持续观察，就绪后自动可用
- **手动启动的独立服务不被桌宠带走**：通过独立服务标记（`.cosyvoice_alife_external`），手动启动的 TTS 在桌宠关闭/Alife 退出后继续运行；手动停止才会结束
- **看门狗误杀修复**：复用/外部服务（本端未持有进程句柄）健康检查连败时，不再误判"进程已死"而强杀重启（用端口进程存活兜底）
- **共享模式不再误杀对方**：跨进程合成锁超时通常是对端桌宠正常占用 GPU，不再强制重启打断对方（仅本端为最后存活客户端时恢复）
- **激活不再阻塞**：`OnAwake` 启动服务后立即返回，由看门狗后台等就绪——机械硬盘加载 10-20 分钟也不会卡住桌宠激活；就绪后自动完成音色校验与预热
- **预热节流**：10 分钟内已有其他桌宠/实例预热过则跳过，避免重复占用 GPU
- 复用已有服务时重置 TTS 失败计数，避免误触发恢复

### v4.1.0

- **新增服务控制（桌宠未启动也能用）**：配置页顶部新增「启动/停止/刷新状态」开关 + 实时状态轮询
  - 手动启动不随 Alife 退出：机械硬盘用户可先点「启动服务」让模型后台加载，之后开桌宠即可秒用
  - 手动停止后，看门狗不会自动重启（直到再次启动或重开桌宠）
  - 桌宠运行时走实例路径（与自动启动共享/独占语义一致）；未运行时走静态路径
- **移除无用配置项**：`流式切片 LimitCount`（插件始终非流式合成，此参数无效）、`API 地址`（由端口自动推导）
- **看门狗不再误杀加载中的服务**：进程存活视为仍在加载（机械硬盘首次加载可能很久），只对已死/僵尸进程做清理重启；`RestartAfterFailures` 默认对齐为 8 并如实生效
- 配置 UI 精简：服务状态徽标支持「运行中 / 加载中 / 未运行」三态

### v4.0.0

- **适配 Alife 4.0.1 框架**：
  - 生命周期改用 `ChatBehaviour`（`OnAwake`/`OnDestroy`），`ISystemEvent` 已在框架中过时
  - `ISpeechModel` 接口迁移至 `Alife.Function.AIModelUtility` 插件
  - `AlifePath.TempFolderPath` 迁移至 `Alife.Foundation` 命名空间
  - 新增插件清单 `manifest.json`（依赖 `Alife.Function.AIModelUtility`）
- 功能与 v1.3.3 完全一致，仅框架适配

### v1.3.3

- **安静日志 QuietLog（默认开）**：日常使用不刷合成过程日志，仅保留错误与重要状态变更；排查问题时可在 UI 关闭

### v1.3.2

- **ZIP 包精简**：打包仅含 `.cs` 源码与 `VERSION.txt`，不再包含 `.json`/`README.md`
- **README 修正**：消除 v1.2.1 重复标题；SharedService 默认值描述改为"关"，与代码一致

### v1.3.1

- **SharedService 默认关闭**：单桌宠更快、无启动跨进程锁卡顿；Alife 退出自动停 Python
- 跨进程锁等 10s 超时 + 首次超时即强制恢复（不再白等 3 分钟）
- UI 新增整合包下载链接（百度/夸克）、小白三步上手教程、各区域详细说明
- 高级路径默认展开、各输入标签补充通俗解释

### v1.3.0

- **移除所有生图兼容代码**：删除 GpuPriorityGuard、ProtectVoicePriority、ImageGenBaseUrl/Ports、soft interrupt 等，恢复 v1.2.0 纯净基线
- **保留清理僵尸进程按钮**：配置 UI 可一键结束残留 Python 并清共享心跳
- 专注 TTS：共享/看门狗/自动恢复/合成全局日志/持锁超时

### v1.2.1

- **语音优先 ProtectVoicePriority（默认开）**：GPU 争抢 / 合成超时 / 持锁过久时，可结束 ComfyUI 等生图进程腾 GPU；**绝不杀 Alife 本体与 CosyVoice**
- 合成全程日志：`开始合成` → `等待锁` → `POST /tts` → `完成 xx ms`
- 进程内 / 跨进程合成锁带超时（默认 60s），避免前一请求挂死后无限转圈
- HTTP 超时计入 `_ttsFailStreak`；连败 / 多次超时触发 Cosy 重启
- 看门狗识别「合成持锁过久」为 GPU 挂死，不再当正常忙碌跳过恢复

### v1.2.0

- **SharedService（默认开）**：多桌宠共享同一 TTS 进程
- 跨进程启动互斥 + 合成串行，避免双开 Python / 并发 500
- 退出时仅最后一位客户端停服；共享模式不绑 KillOnClose Job
- 看门狗识别其它桌宠合成中，避免误重启
- 启动时设置 `OMP_NUM_THREADS` / `MKL_NUM_THREADS=4`，稳延迟
- **TTS 连败检测 + 自动重启**：CUDA 上下文被 ComfyUI 等冲坏后，看门狗检测连续 3 次合成失败并自动重启服务
- **死进程自动清理**：Alife 启动时检测到服务进程已死，自动 ForceCleanup 清除残留并重起，不再卡初始化

### v1.1.1

- 合成前过滤纯标点 / 空白等不可朗读文本，避免 Cosy 稳定 500
- 500 分类：拒读不重试；真忙短退避（最多 4 次）
- 无人等待时停止 InFlight 重试，避免取消后仍打满重试
- 失败 / 忙时日志带截断原文，便于对照

### v1.1.0

- 端口探测改为监听表查询，修复 `TcpClient.ConnectAsync` 超时导致的未观察 Socket 异常
- 日志改为 `Console` 中文输出，去掉 `info: …CosyVoiceSpeechModel[0]` 框架前缀
- 风格指令留空时不传 `instruct`，使用音色默认语气
- 服务启动 / 健康检查 / 残留清理 / 预热等行为整理与稳定性增强
- 配置 UI 重做为「Cosy 星语」粉紫流光主题：声波动画、状态脉冲徽标、音色胶囊按钮、Mode/AutoStart 芯片选择、高级折叠与页脚星语标语

### v1.0.1

- 新增 UI 配置面板、自定义服务路径、自动启动守护、健康检查与重启、请求重试与缓存

### v1.0.0

- 初始版本
